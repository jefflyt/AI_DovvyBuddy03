# Database connection string (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/dovvybuddy"

# GROQ API key for LLM inference and embeddings (will evaluate other models after testing)
GROQ_API_KEY="your-groq-api-key"

# Google AI API key for Gemini model (chat orchestration via Google ADK)
GOOGLE_AI_API_KEY="your-google-ai-api-key"

# Brevo API key for email delivery (lead capture)
BREVO_API_KEY="your-brevo-api-key"

# Public-facing app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Secret key for session encryption (min 32 chars recommended)
SESSION_SECRET="your-session-secret"
